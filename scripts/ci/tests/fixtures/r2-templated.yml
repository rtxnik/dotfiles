name: templated
on: [push]
permissions:
  contents: read
jobs:
  a:
    runs-on: ubuntu-latest
    strategy: { matrix: { action: ['actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683'] } }
    steps:
      - uses: ${{ matrix.action }}
