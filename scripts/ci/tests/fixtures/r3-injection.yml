name: injection
on: [pull_request_target]
permissions:
  contents: read
jobs:
  x:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.event.pull_request.title }}"   # injectable — R3 must flag
      - run: |
          NAME="${{ github.head_ref }}"                       # injectable — R3 must flag
          echo "$NAME"
